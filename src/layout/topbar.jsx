import React from 'react';
import ReactDOM from 'react-dom';
import {createStore} from 'redux';

export default function(){

    return (
        <nav>
            <button>Pick language</button>
        </nav>
    )

}
